@page "/practice"
@inject NavigationManager Nav

@* The landing page and older links point at /practice. Level select now lives
   on the home screen, so send them there rather than keeping two pickers. *@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("", replace: true);
}
